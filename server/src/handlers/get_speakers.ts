import { type User } from '../schema';

export const getSpeakers = async (): Promise<User[]> => {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all users with 'speaker' role from the database.
    // Used for populating speaker dropdown in seminar creation/editing forms.
    return [];
};